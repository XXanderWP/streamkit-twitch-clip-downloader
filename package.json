{
  "name": "streamkit-twitch-clip-downloader",
  "version": "1.0.3",
  "private": true,
  "description": "Download public Twitch clips and VODs via yt-dlp.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "@rocketman-streamkit/types": "^1.0.19",
    "typescript": "^5.8.3"
  }
}
